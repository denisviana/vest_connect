<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/raleway_regular</item>
        <item name="android:colorControlNormal">@color/colorPrimary</item>
    </style>

    <style name="SecondaryStyle" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <item name="colorControlNormal">#fff</item>
    </style>

    <style name="RegisterToolbarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">#fff</item>
    </style>

    <style name="Fullscreen" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="SecondaryEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="background">@drawable/edit_text_state_secondary</item>
        <item name="android:lines">1</item>
    </style>

</resources>